<?xml version="1.0" encoding="utf-8"?>
<!--
  Configuración de seguridad de red para Tutor IA.
  Permite tráfico HTTPS a Supabase (*.supabase.co) en todos los entornos.
  Permite HTTP solo en redes locales (192.168.x.x) para desarrollo con Metro.
-->
<network-security-config>
    <!-- Supabase: siempre HTTPS, certificados de la CA del sistema -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">supabase.co</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Metro Bundler (desarrollo local): permite HTTP en localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!-- Configuración base: HTTPS para todo lo demás -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
